// import { useFrame } from "@react-three/fiber"
// import  * as THREE from "three";
// import { useThree } from "@react-three/fiber";
// import { useState } from "react";



// export default function rotatingSun({SunRef}){
    
//     useFrame(({clock})=>{
//         const time = clock.getElapsedTime()// Ottieni il tempo trascorso
// if(SunRef.current)
//     SunRef.current.position.y = Math.sin(time) * 0.4
// SunRef.current.position.x = Math.sin(time) * 0.1
// SunRef.current.position.z = Math.sin(time) * 0.3


//     })
// }